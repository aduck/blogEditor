extends layout.pug
block header
	title #{blog.name}-所有标签
	link(href="../css/style.css",rel="stylesheet")
block content
	.container
		each item in tags
			a(href=`/tag/${item}`) #{item}
		else
			| 没有数据